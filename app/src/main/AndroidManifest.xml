<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chuhieu.kiot_demo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />



    <application
        android:allowBackup="true"
        android:icon="@mipmap/namishop"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/namishop_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.login">
        <activity android:name=".BarCode.BarCodeActivity"></activity>
        <activity android:name=".Login.activity.SplashscreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Login.activity.LoginActivity"
            android:theme="@style/AppTheme.login" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.kiot" />
        <!--<meta-data-->
            <!--android:name="com.google.android.gms.vision.DEPENDENCIES"-->
            <!--android:value="barcode" />-->

    </application>

</manifest>